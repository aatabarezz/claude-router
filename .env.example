# PII Vault Secret
# Used for deterministic token generation and encryption of PII values in the vault.
# Must be a strong, random string (minimum 32 characters recommended).
# In production, set via environment variable or secure secrets manager (AWS Secrets Manager, etc).
# For development, you can generate one with: openssl rand -hex 32
PII_VAULT_SECRET=87fb090087b6357fc205b4661ab16bcf64d34b4256237bce12720c15c408cf11
